{
  "url": "/v1/ad-library/google/company-ads",
  "method": "GET",
  "qs": {
    "advertiser": "{{parameters.advertiser}}",
    "country": "{{parameters.country}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
