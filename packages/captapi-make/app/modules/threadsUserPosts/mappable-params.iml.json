[
  {
    "name": "url",
    "type": "text",
    "label": "URL",
    "required": true,
    "help": "Threads profile URL or @handle, e.g. https://threads.net/@username."
  },
  {
    "name": "limit",
    "type": "number",
    "label": "Limit",
    "required": false,
    "help": "Max items to return. Default 20, max 100. Billed per result."
  },
  {
    "name": "cache",
    "type": "boolean",
    "label": "Cache",
    "required": false,
    "help": "Set true to serve from the 24h response cache. Default false — always fetch fresh data."
  }
]
