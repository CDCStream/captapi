{
  "url": "/v1/age-gender",
  "method": "GET",
  "qs": {
    "name": "{{parameters.name}}",
    "names": "{{parameters.names}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
