[
  {
    "name": "name",
    "type": "text",
    "label": "Name",
    "required": true,
    "help": "First name, or fallback when names is omitted."
  },
  {
    "name": "names",
    "type": "text",
    "label": "Names",
    "required": false,
    "help": "Optional comma-separated list of names."
  },
  {
    "name": "cache",
    "type": "boolean",
    "label": "Cache",
    "required": false,
    "help": "Set true to serve from the 24h response cache. Default false — always fetch fresh data."
  }
]
