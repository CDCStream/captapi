{
  "url": "/v1/instagram/highlights-details",
  "method": "GET",
  "qs": {
    "id": "{{parameters.id}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
