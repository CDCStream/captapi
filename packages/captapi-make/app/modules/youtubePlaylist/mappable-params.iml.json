[
  {
    "name": "url",
    "type": "text",
    "label": "URL",
    "required": true,
    "help": "YouTube playlist URL, e.g. https://youtube.com/playlist?list=ID."
  },
  {
    "name": "limit",
    "type": "number",
    "label": "Limit",
    "required": false,
    "help": "Max items to return. Default 50, max 500. Billed per result."
  },
  {
    "name": "fast",
    "type": "boolean",
    "label": "Fast",
    "required": false,
    "help": "Set true to use YouTube RSS for faster results with less detailed metadata. Leave false when viewCount/duration quality matters."
  },
  {
    "name": "cache",
    "type": "boolean",
    "label": "Cache",
    "required": false,
    "help": "Set true to serve from the 24h response cache. Default false — always fetch fresh data."
  }
]
