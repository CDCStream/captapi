{
  "url": "/v1/youtube/playlist",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "limit": "{{parameters.limit}}",
    "fast": "{{parameters.fast}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
