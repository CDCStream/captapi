{
  "url": "/v1/youtube/playlist-videos",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "limit": "{{parameters.limit}}",
    "fast": "{{parameters.fast}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
