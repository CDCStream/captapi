{
  "url": "/v1/amazon-shop/page",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "marketplace": "{{parameters.marketplace}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
