{
  "url": "/v1/instagram/channel-reels",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "limit": "{{parameters.limit}}",
    "cursor": "{{parameters.cursor}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
