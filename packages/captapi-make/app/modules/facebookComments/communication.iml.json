{
  "url": "/v1/facebook/comments",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
