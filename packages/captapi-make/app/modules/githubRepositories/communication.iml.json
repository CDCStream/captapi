{
  "url": "/v1/github/repositories",
  "method": "GET",
  "qs": {
    "username": "{{parameters.username}}",
    "limit": "{{parameters.limit}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
