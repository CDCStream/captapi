[
  {
    "name": "repo",
    "type": "text",
    "label": "Repo",
    "required": true,
    "help": "Repository URL or owner/name."
  },
  {
    "name": "state",
    "type": "text",
    "label": "State",
    "required": false,
    "help": "open, closed, or all. Default open."
  },
  {
    "name": "limit",
    "type": "number",
    "label": "Limit",
    "required": false,
    "help": "Max items to return. Default 30, max 100. Billed per result."
  },
  {
    "name": "cache",
    "type": "boolean",
    "label": "Cache",
    "required": false,
    "help": "Responses are cached for 24 hours by default. Set false to bypass the cache and always fetch fresh data (default true)."
  }
]
