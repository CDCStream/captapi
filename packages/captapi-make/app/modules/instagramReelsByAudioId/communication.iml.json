{
  "url": "/v1/instagram/reels-by-audio-id",
  "method": "GET",
  "qs": {
    "audio_id": "{{parameters.audio_id}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
