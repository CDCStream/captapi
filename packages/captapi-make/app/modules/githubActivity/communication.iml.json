{
  "url": "/v1/github/activity",
  "method": "GET",
  "qs": {
    "username": "{{parameters.username}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
