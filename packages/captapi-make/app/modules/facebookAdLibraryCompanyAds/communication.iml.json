{
  "url": "/v1/ad-library/facebook/company-ads",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "country": "{{parameters.country}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
