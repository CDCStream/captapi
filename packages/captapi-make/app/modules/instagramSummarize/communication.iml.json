{
  "url": "/v1/instagram/summarize",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "language": "{{parameters.language}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
