{
  "url": "/v1/youtube/trending-shorts",
  "method": "GET",
  "qs": {
    "q": "{{parameters.q}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
