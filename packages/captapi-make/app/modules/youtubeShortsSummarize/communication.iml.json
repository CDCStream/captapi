{
  "url": "/v1/youtube/shorts/summarize",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "language": "{{parameters.language}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
