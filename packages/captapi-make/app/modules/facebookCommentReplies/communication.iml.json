{
  "url": "/v1/facebook/comment-replies",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "comment_id": "{{parameters.comment_id}}",
    "limit": "{{parameters.limit}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
