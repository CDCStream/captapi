[
  {
    "name": "q",
    "type": "text",
    "label": "Search Query",
    "required": true,
    "help": "City/place search query, e.g. Austin."
  },
  {
    "name": "limit",
    "type": "number",
    "label": "Limit",
    "required": false,
    "help": "Max items to return. Default 10, max 50. Flat 17 credits per call."
  },
  {
    "name": "details",
    "type": "text",
    "label": "Details",
    "required": false,
    "help": "Set true to include latitude/longitude per location (slower; doubles cost to 34 credits)."
  },
  {
    "name": "cache",
    "type": "boolean",
    "label": "Cache",
    "required": false,
    "help": "Set true to serve from the 24h response cache. Default false — always fetch fresh data."
  }
]
