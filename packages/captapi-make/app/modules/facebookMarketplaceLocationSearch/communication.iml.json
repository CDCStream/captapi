{
  "url": "/v1/facebook/marketplace-location-search",
  "method": "GET",
  "qs": {
    "q": "{{parameters.q}}",
    "limit": "{{parameters.limit}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
