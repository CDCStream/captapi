[
  {
    "name": "userId",
    "type": "text",
    "label": "UserId",
    "required": true,
    "help": "Instagram numeric user ID (e.g. 314216). A profile URL, @handle, or username is also accepted and resolved automatically."
  },
  {
    "name": "cache",
    "type": "boolean",
    "label": "Cache",
    "required": false,
    "help": "Set true to serve from the 24h response cache. Default false — always fetch fresh data."
  }
]
