{
  "url": "/v1/instagram/basic-profile",
  "method": "GET",
  "qs": {
    "userId": "{{parameters.userId}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
