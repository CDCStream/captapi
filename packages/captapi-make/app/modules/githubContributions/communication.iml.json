{
  "url": "/v1/github/contributions",
  "method": "GET",
  "qs": {
    "username": "{{parameters.username}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
