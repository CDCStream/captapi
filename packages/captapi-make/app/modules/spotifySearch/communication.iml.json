{
  "url": "/v1/spotify/search",
  "method": "GET",
  "qs": {
    "q": "{{parameters.q}}",
    "type": "{{parameters.type}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
