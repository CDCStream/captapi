{
  "url": "/v1/linkedin/search-posts",
  "method": "GET",
  "qs": {
    "q": "{{parameters.q}}",
    "sort": "{{parameters.sort}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
