{
  "url": "/v1/instagram/story-highlights",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
