{
  "url": "/v1/instagram/trending-reels",
  "method": "GET",
  "qs": {
    "country": "{{parameters.country}}",
    "limit": "{{parameters.limit}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
