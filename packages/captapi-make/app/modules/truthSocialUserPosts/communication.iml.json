{
  "url": "/v1/truth-social/user-posts",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
