{
  "url": "/v1/youtube/channel-videos",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "limit": "{{parameters.limit}}",
    "fast": "{{parameters.fast}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
