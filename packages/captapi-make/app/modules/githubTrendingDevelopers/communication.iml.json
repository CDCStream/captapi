{
  "url": "/v1/github/trending-developers",
  "method": "GET",
  "qs": {
    "q": "{{parameters.q}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
