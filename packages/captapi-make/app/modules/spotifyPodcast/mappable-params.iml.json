[
  {
    "name": "url",
    "type": "text",
    "label": "URL",
    "required": true,
    "help": "Spotify URL, URI, or ID."
  },
  {
    "name": "limit",
    "type": "number",
    "label": "Limit",
    "required": false,
    "help": "Max items to return. Default 20, max 50. Billed per result."
  },
  {
    "name": "cache",
    "type": "boolean",
    "label": "Cache",
    "required": false,
    "help": "Responses are cached for 24 hours by default. Set false to bypass the cache and always fetch fresh data (default true)."
  }
]
