{
  "url": "/v1/tiktok/summarize",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "language": "{{parameters.language}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
