{
  "url": "/v1/youtube/hashtag-search",
  "method": "GET",
  "qs": {
    "q": "{{parameters.q}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
