{
  "url": "/v1/ad-library/google/advertiser-search",
  "method": "GET",
  "qs": {
    "q": "{{parameters.q}}",
    "country": "{{parameters.country}}",
    "limit": "{{parameters.limit}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
