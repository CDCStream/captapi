{
  "url": "/v1/github/followers",
  "method": "GET",
  "qs": {
    "username": "{{parameters.username}}",
    "limit": "{{parameters.limit}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
