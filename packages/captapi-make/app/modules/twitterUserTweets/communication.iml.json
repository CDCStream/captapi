{
  "url": "/v1/twitter/user-tweets",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "limit": "{{parameters.limit}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
