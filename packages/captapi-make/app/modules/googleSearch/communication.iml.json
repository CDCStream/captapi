{
  "url": "/v1/google/search",
  "method": "GET",
  "qs": {
    "q": "{{parameters.q}}",
    "country": "{{parameters.country}}",
    "language": "{{parameters.language}}",
    "limit": "{{parameters.limit}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
