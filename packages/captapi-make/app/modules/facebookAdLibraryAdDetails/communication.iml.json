{
  "url": "/v1/ad-library/facebook/ad-details",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
