{
  "url": "/v1/youtube/shorts/transcript",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "language": "{{parameters.language}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
