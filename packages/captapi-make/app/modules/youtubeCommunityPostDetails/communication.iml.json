{
  "url": "/v1/youtube/community-post-details",
  "method": "GET",
  "qs": {
    "url": "{{parameters.url}}",
    "cache": "{{parameters.cache}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
