# Endpoint Competitive Gap Plan

Captapi vs ScrapeCreators. **Additive fields only** — never rename/remove keys customers already parse.

## Philosophy

- Keep envelope + camelCase + stable normalized shape
- Do not dump raw GraphQL / aweme by default
- Prefer exact counts when available; flag approximate otherwise
- Always expose stable platform IDs + real timestamps when raw has them

---

## #1-2 YouTube / TikTok video-details

See also VIDEO-DETAILS-GAP-PLAN.md. Phase 1 largely shipped (2026-08-01).

---

## #3 Instagram GET /v1/instagram/channel-details

Shipped additive (2026-08-01). Deferred: embedded recent posts, relatedProfiles.

---

## #4 YouTube GET /v1/youtube/comments

Shipped additive authorChannelId / publishedTime / heart fix. Deferred: order=top|newest.

---

## #5 TikTok GET /v1/tiktok/channel-details

Shipped additive id/secUid/createTime/commerce/settings (2026-08-01).

---

## #6 Transcript (YouTube + TikTok)

Shipped additive body `source` (+ YT isAutoGenerated / availableLanguages). Never rename segment keys.

---

## #7 Facebook GET /v1/ad-library/facebook/search

**Constraint:** keep existing ad keys (`platform`, `id`, `url`, `text`, `headline`, `cta`, `landingUrl`, `adFormat`, `firstShown`, `lastShown`, `impressions`, `spend`, `country`, `advertiser{id,name,url,logo}`, `media[]`). Additive only.

| Field / param | Priority | Status |
|---|---|---|
| status=ACTIVE\|INACTIVE\|ALL (default ACTIVE) | P0 | Shipped |
| media_type / ad_type / search_type / sort_by | P0 | Shipped |
| start_date / end_date (YYYY-MM-DD) | P0 | Shipped |
| isActive | P0 | Shipped additive |
| publisherPlatforms | P0 | Shipped additive |
| cards[] (carousel) | P0 | Shipped additive |
| images[] / videos[] typed (keep media[]) | P0 | Shipped additive |
| spendRange / impressionsRange (keep string spend/impressions) | P0 | Shipped additive |
| Docs: spend/impressions political-only warning | P0 | Shipped in catalog copy |
| disclaimerLabel / byline / brandedContent | P1 | Shipped additive |
| pageLikeCount / pageCategories / pageEntityType | P1 | Shipped additive |
| caption / linkDescription / reachEstimate / totalActiveTime / politicalCountries | P1 | Shipped additive |
| searchResultsCount / hasMore / nextCursor | P1 | Shipped (nextCursor null until GraphQL paging) |
| Document max 200 | P1 | Shipped |
| True cursor pagination beyond first HTML page | P2 | Deferred |
| POST bulk variant | P2 | Deferred |

---

## Cross-cutting

| Issue | Action |
|---|---|
| Stable IDs missing | Checklist for every entity normalizer |
| fetchedAt | Envelope middleware + per-payload where needed |
| Approximate counts | *IsApproximate / engagement.isApproximate |
| CDN expiry | mediaUrlsExpireAt / caption expiresAt |
| Schema consistency | Align additive flags across platforms |

---

## Next comparison candidates

1. LinkedIn Profile
2. Reddit
3. Facebook Ad Library company-ads / ad-details (same enricher)
4. AI summary differentiator